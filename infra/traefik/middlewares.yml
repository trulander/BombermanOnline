http:
  middlewares:
    # Заголовки безопасности
    security-headers:
      headers:
        frameDeny: true
        sslRedirect: false
        browserXssFilter: true
        contentTypeNosniff: true
        forceSTSHeader: false
        stsIncludeSubdomains: true
        stsPreload: true
        stsSeconds: 31536000
    
    # Middleware для сжатия ответов
    compress:
      compress: {}
    
    # Middleware для удаления префикса /api
    strip-api-prefix:
      stripPrefix:
        prefixes: 
          - "/api"

    # Middleware для удаления префикса /logs
    strip-logs-prefix:
      stripPrefix:
        prefixes:
          - "/logs"
        
    # Middleware для WebSocket соединений
    websocket:
      headers:
        customRequestHeaders:
          X-Forwarded-Proto: "ws"
    
    # Middleware для проверки JWT токена
    auth-check:
      forwardAuth:
        address: "http://auth-service:5003/api/v1/auth/check"
        authResponseHeaders:
          - "X-User-ID"
          - "X-User-Role"
          - "X-User-Email"
          - "X-User-Name"
        trustForwardHeader: true
    
    # Middleware для редиректа неавторизованных пользователей
    auth-redirect:
      forwardAuth:
        address: "http://auth-service:5003/api/v1/auth/check?redirect=true"
        authResponseHeaders:
          - "X-User-ID"
          - "X-User-Role"
          - "X-User-Email"
          - "X-User-Name"
        trustForwardHeader: true

#
#    my-traefik-plugin-AdminAPI_WebUI1:
#      plugin:
#        traefik-plugin-AdminAPI_WebUI1:
#          root: /tmp/